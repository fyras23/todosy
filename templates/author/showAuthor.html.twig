<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">

    <div class="container mx-auto p-8 mt-9">
        <div class="max-w-lg mx-auto bg-white rounded-lg shadow-md overflow-hidden">
            <img class="w-full h-64 object-cover" src="{{ author.picture }}" alt="{{ author.username }}">

            <div class="p-6">
                <h1 class="text-3xl font-bold text-gray-800 mb-4">{{ author.username|upper }}</h1>
                <p class="text-gray-600 mb-4"><strong>Email:</strong> {{ author.email }}</p>
                <p class="text-gray-700 font-semibold mb-6"><strong>Number of Books:</strong> {{ author.nb_books }}</p>

                <a href="{{ path('author_list') }}" class="inline-block bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600">
                    Back to list
                </a>
            </div>
        </div>
    </div>

</body>
</html>
